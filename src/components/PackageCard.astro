---
// src/components/PackageCard.astro
import { TbApi } from "react-icons/tb";
import NpmStats from "./NpmStats.tsx";

interface Props {
  title: string;
  description: string;
  guideLink: string;
  sourceLink: string;
  type: "api" | "auth" | "baileys";
  npmPackage?: string;
}

const { title, description, guideLink, sourceLink, type, npmPackage } = Astro.props;
---

<div class="flex flex-col bg-white dark:bg-[#111113] border border-gray-200 dark:border-gray-800 rounded-[1.5rem] md:rounded-[2rem] p-6 md:p-8 shadow-sm hover:shadow-md transition-shadow h-full">
  
  <div class="flex items-center justify-center w-[80px] h-[80px] md:w-[100px] md:h-[100px] rounded-2xl md:rounded-[1.5rem] overflow-hidden shrink-0 bg-gray-50 dark:bg-[#1a1a1c]">
    {type === 'api' && (
      <div class="w-full h-full bg-yellow-400 flex items-center justify-center">
        <TbApi className="w-10 h-10 md:w-14 md:h-14 text-black" />
      </div>
    )}
    {type === 'auth' && (
      <img src="/ht_iconAccounts.svg" alt="Heavstal Auth Logo" class="w-full h-full object-contain p-1.5 md:p-2" />
    )}
    {type === 'baileys' && (
      <img src="/baileys.svg" alt="HT Baileys Logo" class="w-full h-full object-contain p-1.5 md:p-2" />
    )}
  </div>

  <div class="flex flex-col gap-1.5 md:gap-2 mt-5 md:mt-6">
    <h3 class="text-xl md:text-2xl font-bold text-gray-900 dark:text-white m-0 leading-none">{title}</h3>
    <NpmStats packageName={npmPackage} client:load />
  </div>

  <p class="text-gray-600 dark:text-gray-400 text-[13px] md:text-sm flex-grow leading-relaxed m-0 mt-4 md:mt-5">
    {description}
  </p>
  
  <div class="flex justify-start items-center gap-3 w-full mt-6 md:mt-8">
    <a 
      href={guideLink} 
      class="no-underline px-5 md:px-6 py-2 md:py-2.5 bg-blue-600 hover:bg-blue-700 text-white text-[13px] md:text-sm font-semibold rounded-full transition-colors"
    >
      Guide
    </a>
    
    <a 
      href={sourceLink} 
      target="_blank" 
      rel="noopener noreferrer"
      class="no-underline px-5 md:px-6 py-2 md:py-2.5 bg-gray-100 dark:bg-gray-800/80 hover:bg-gray-200 dark:hover:bg-gray-700 text-gray-900 dark:text-gray-100 text-[13px] md:text-sm font-semibold rounded-full transition-colors"
    >
      Source
    </a>
  </div>
</div>
