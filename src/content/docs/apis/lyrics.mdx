---
title: "Music Lyrics API"
description: "Fetch accurate song lyrics and metadata using a multi-source engine."
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

This API provides a high-availability lyrics fetcher. It aggregates data from open-source and metadata providers to ensure you get accurate, synced lyrics and album art.

## Endpoint

```text
POST https://heavstal.com.ng/api/v1/lyrics
```

## Request Parameters

The following parameters are accepted in the request body:

| Field | Type | Required | Description |
| :--- | :--- | :--- | :--- |
| `query` | `string` | **Yes** | The song title and artist (e.g., "Mockingbird Eminem"). |

## Request Examples

Choose your preferred method to interact with the Lyrics API:

<Tabs>
  <TabItem label="cURL">
  ```bash
  curl -X POST https://heavstal.com.ng/api/v1/lyrics \
  -H "Content-Type: application/json" \
  -H "x-api-key: <YOUR_API_KEY>" \
  -d '{"query": "Billie Eilish Bad Guy"}'
  ```
  </TabItem>
  <TabItem label="JavaScript (Fetch)">
  ```javascript
  const response = await fetch('https://heavstal.com.ng/api/v1/lyrics', {
    method: 'POST',
    headers: { 
      'Content-Type': 'application/json',
      'x-api-key': '<YOUR_API_KEY>' 
    },
    body: JSON.stringify({ 
      query: 'Billie Eilish Bad Guy' 
    })
  });

  const res = await response.json();
  console.log(res.data.lyrics);
  ```
  </TabItem>
  <TabItem label="Heavstal SDK">
  ```javascript
  import { search } from '@heavstal/api';

  const song = await search.lyrics("Kendrick Lamar DNA");
  console.log(song.lyrics);
  ```
  </TabItem>
  <TabItem label="Python">
  ```python
  import requests

  url = "https://heavstal.com.ng/api/v1/lyrics"
  headers = {
      "Content-Type": "application/json",
      "x-api-key": "<YOUR_API_KEY>"
  }
  payload = {
      "query": "Billie Eilish Bad Guy"
  }

  response = requests.post(url, headers=headers, json=payload)
  print(response.json()["data"]["lyrics"])
  ```
  </TabItem>
</Tabs>

## Response Examples

<Tabs>
  <TabItem label="Success Response">
  ```json
  {
    "status": "success",
    "creator": "HEAVSTAL TECH",
    "data": {
      "title": "bad guy",
      "artist": "Billie Eilish",
      "image": "https://i.scdn.co/image/...",
      "url": "https://lrclib.net/api/get/...",
      "lyrics": "White shirt now red, my bloody nose..."
    }
  }
  ```
  </TabItem>
  <TabItem label="Error Response">
  ```json
  {
    "status": "error",
    "error": "Lyrics not found for the requested song."
  }
  ```
  </TabItem>
</Tabs>