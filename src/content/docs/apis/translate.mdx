---
title: "Translator API"
description: "Auto-detects and translates upto 240+ languagea into English."
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

This API provides automatically detects the source language of your provided text and translates it into English. Supporting upto 240+ languages.

## Endpoint

```text
POST https://heavstal.com.ng/api/v1/translate
```

## Request Parameters

The following parameters are accepted in the request body:

| Field | Type | Required | Description |
| :--- | :--- | :--- | :--- |
| `text` | `string` | **Yes** | The text you want to translate. |

## Request Examples

<Tabs>
  <TabItem label="cURL">
  ```bash
  curl -X POST https://heavstal.com.ng/api/v1/translate \
  -H "Content-Type: application/json" \
  -H "x-api-key: <YOUR_API_KEY>" \
  -d '{"text": "Bonjour tout le monde"}'
  ```
  </TabItem>
  <TabItem label="JavaScript (Fetch)">
  ```javascript
  const response = await fetch('https://heavstal.com.ng/api/v1/translate', {
    method: 'POST',
    headers: { 
      'Content-Type': 'application/json',
      'x-api-key': '<YOUR_API_KEY>' 
    },
    body: JSON.stringify({ text: "Bonjour tout le monde" })
  });

  const res = await response.json();
  console.log(res.data.translated);
  ```
  </TabItem>
  <TabItem label="Heavstal SDK">
  ```javascript
  import { tools } from '@heavstal/api';

  const translation = await tools.translate("Bonjour tout le monde");
  console.log(translation.data.translated); // "Hello everyone"
  ```
  </TabItem>
  <TabItem label="Python">
  ```python
  import requests

  url = "https://heavstal.com.ng/api/v1/translate"
  headers = {
      "Content-Type": "application/json",
      "x-api-key": "<YOUR_API_KEY>"
  }
  payload = {
      "text": "Bonjour tout le monde"
  }

  response = requests.post(url, headers=headers, json=payload)
  print(response.json()["data"])
  ```
  </TabItem>
</Tabs>

## Response Examples

<Tabs>
  <TabItem label="Success Response">
  ```json
  {
    "status": "success",
    "creator": "HEAVSTAL TECH",
    "data": {
      "original": "Bonjour tout le monde",
      "translated": "Hello everyone",
      "source_language": "fr",
      "pronunciation": "bon-zhoor..."
    }
  }
  ```
  </TabItem>
  <TabItem label="Error Response">
  ```json
  {
    "status": "error",
    "error": "Could not detect source language."
  }
  ```
  </TabItem>
</Tabs>