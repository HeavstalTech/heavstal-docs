---
title: "Live Crypto Data"
description: "Live cryptocurrency data."
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

This API provides real-time pricing, 24-hour percentage changes, market cap, and volume for the top 200 cryptocurrencies. To ensure extreme speed and stability, responses are cached for 5 minutes for optimal speed.

:::note
Crypto prices are aggregated from third-party providers and may be delayed or inaccurate due to caching. This API is intended for informational purposes and should not be used as the sole source for trading or financial decisions.
:::

## Endpoint

```text
POST https://heavstal.com.ng/api/v1/crypto
```

## Request Parameters

The following parameters are accepted in the request body:

| Field | Type | Required | Description | Example |
| :--- | :--- | :--- | :--- | :--- |
| `limit` | `integer` | Optional | Number of coins to return (Max 20. Default is 10). | `5` |
| `symbol` | `string` | Optional | Filter results by a specific coin symbol. | `"BTC"` |

## Request Examples

<Tabs>
  <TabItem label="cURL">
  ```bash
  curl -X POST https://heavstal.com.ng/api/v1/crypto \
  -H "Content-Type: application/json" \
  -H "x-api-key: <YOUR_API_KEY>" \
  -d '{"limit": 2, "symbol": "ETH"}'
  ```
  </TabItem>
  <TabItem label="JavaScript (Fetch)">
  ```javascript
  const response = await fetch('https://heavstal.com.ng/api/v1/crypto', {
    method: 'POST',
    headers: { 
      'Content-Type': 'application/json',
      'x-api-key': '<YOUR_API_KEY>' 
    },
    body: JSON.stringify({ 
      limit: 2,
      symbol: "ETH" 
    })
  });

  const res = await response.json();
  console.log(res.data);
  ```
  </TabItem>
  <TabItem label="Heavstal SDK">
  ```javascript
  import { search } from '@heavstal/api';

  const btc = await search.crypto(1, "BTC");
  console.log(btc.data[0].price_usd);
  ```
  </TabItem>
  <TabItem label="Python">
  ```python
  import requests

  url = "https://heavstal.com.ng/api/v1/crypto"
  headers = {
      "Content-Type": "application/json",
      "x-api-key": "<YOUR_API_KEY>"
  }
  payload = {
      "limit": 2,
      "symbol": "ETH"
  }

  response = requests.post(url, headers=headers, json=payload)
  print(response.json()["data"])
  ```
  </TabItem>
</Tabs>

## Response Examples

<Tabs>
  <TabItem label="Success Response">
  ```json
  {
    "status": "success",
    "creator": "HEAVSTAL TECH",
    "provider": "CoinGecko",
    "cached": true,
    "cache_ttl": 248,
    "timestamp": "2026-06-29T10:08:45Z",
    "data": [
      {
        "id": "ethereum",
        "rank": 2,
        "symbol": "ETH",
        "name": "Ethereum",
        "price_usd": "3450.21",
        "change_24h_percent": "1.45",
        "market_cap_usd": "415000000000",
        "volume_24h_usd": "12500000000"
      }
    ]
  }
  ```
  </TabItem>
  <TabItem label="Error Response">
  ```json
  {
    "status": "error",
    "error": "Symbol not found in top 200 cryptocurrencies."
  }
  ```
  </TabItem>
</Tabs>